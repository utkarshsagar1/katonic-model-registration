# Meralco Proprietary Model Dependencies (Best Guess)
# Adjusted versions to satisfy PyCaret 3.2.0 dependency chain.

# Core Machine Learning Libraries (Required for loading the .pkl files)
numpy==1.23.5    # Adjusted for Scipy 1.10.1 compatibility
scipy==1.10.1    # FIX: Set to exactly satisfy PyCaret 3.2.0 (~=1.10.1)
pandas==1.5.3    # Fixed previously to satisfy PyCaret (< 2.0.0)
scikit-learn==1.2.0 # FIX: Downgraded to satisfy PyCaret and Scipy 1.10.1
joblib==1.3.2
threadpoolctl==3.2.0

# Web Framework (Required for the API endpoint in app.py)
flask==2.3.3

# MLOps/Tracking Library (REQUIRED for 'import mlflow' in register_model.py)
mlflow==2.10.2

# Model Creation Library (REQUIRED for loading the .pkl files)
pycaret==3.2.0

# Other potential packages your models might need:
# If you confirm they used other libraries, add them here.
# requests==2.31.0